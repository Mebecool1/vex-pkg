[package]
name = "vex-pkg"
version = "0.2.4"
edition = "2024"
description = "A tiny decentralized package manager that anyone can host a server for easily.."
exclude = [
  "*.tar",
  "target/"
]
license = "Apache-2.0"
[dependencies]
tiny_http = "0.12.0"
ureq = "3.4.0"


